## iOS Dev Weekly — Issue #25

### Opening

Shared state in SwiftUI can quietly wreck iteration speed — especially when previews re-evaluate entire view trees during interactions. This week I walked a repo from sluggish previews to sub-second turns by changing ownership and tightening preview fixtures; the fixes are small, surgical, and repeatable.

---

### This Week's Big Story

**Fixing Slow SwiftUI Previews in Xcode**

A shared @Observable reference owned by ancestor views can make View.body re-evaluate far more often than you expect, killing preview iteration. The guidance below focuses on per-repo changes you can apply now to reduce unnecessary work and speed up previews. Read on for concrete fixes and a checklist you can run in minutes.

---

### Trend Signals

• Six years refining maps on watchOS highlights that even mature platform features keep evolving and need long-term engineering investment — expect continued low-level optimizations and trade-offs for small screens. [Source: HackerNews]  
• Apple's SHARP running in the browser via ONNX runtime web shows ML tools are increasingly portable, which could influence on-device vs. server inference decisions for UX experiments. [Source: HackerNews]  
• The macOS-created tar files extraction issue reminds teams that cross-platform tooling quirks still bite build and CI pipelines; keep an eye on archive formats when sharing artifacts. [Source: HackerNews]

---

### Swift Snippet of the Week

```swift
import SwiftUI

struct ItemRow: View {
    let title: String
    var body: some View { Text(title).padding() }
}

struct ItemRow_Previews: PreviewProvider {
    static var previews: some View {
        ItemRow(title: "Preview")
    }
}
```

This tiny preview fixture encodes a principle: keep preview inputs minimal and focused so Xcode can render quickly and your iteration loop stays tight.

---

### Community Picks

More community links next issue.


---

### Until Next Time

If slow previews are costing you focus, try the checklist in the full article: tighten preview fixtures, pin BUILD_ACTIVE_ARCH_ONLY for debug, and audit any shared @Observable ownership that forces ancestor re-evaluations. Hit reply with your repo patterns or drop a note on LinkedIn — I want to know whether you trace on-device change impact before or after changing ownership models. Forward this to a teammate who’s been fighting slow Xcode previews.
