## iOS Dev Weekly — Issue #16

### Opening

Converting global singletons into injectable services is trending from "best practice" to "hard lesson" in many SwiftUI codebases. Runtime surprises—missing environment values, duplicated ObservableObject owners, or cache/state mismatches—are exposing thin spots in how teams model ownership and lifecycle.

---

### This Week's Big Story

**Dependency Injection Patterns for SwiftUI Apps**

Converting globals to injectables often surfaces runtime failures that only appear under specific navigation or device profiles. The core engineering problem is ownership: who creates, who owns, and who observes a service in a view hierarchy. Solve that and you remove a large class of intermittent bugs and test gaps.

---

### Trend Signals

• Apple reaches another cultural milestone with renewed attention on platform history and tooling evolution — expect continued scrutiny of platform behavior. [Source: HackerNews]  
• Small, focused iOS apps that strip features (e.g., Dull) suggest users and teams still value predictability over hyper-personalized feeds; this matters when you choose what services to make app-wide. [Source: HackerNews]  
• App store policy frictions continue to surface as Apple removes edge-case apps, reminding teams to validate runtime behaviors against App Store rules and platform expectations. [Source: HackerNews]  
• Swift language progress continues with Swift 6.3 and related updates, which may alter concurrency and actor patterns you use for injectable services. [Source: Swift.org Blog]

---

### Swift Snippet of the Week

```swift
import SwiftUI

private struct CompactLayoutKey: EnvironmentKey {
    static let defaultValue: Bool = false
}

extension EnvironmentValues {
    var isCompactLayout: Bool {
        get { self[CompactLayoutKey.self] }
        set { self[CompactLayoutKey.self] = newValue }
    }
}

extension View {
    func compactLayout(_ enabled: Bool) -> some View {
        environment(\.isCompactLayout, enabled)
    }
}

struct ListRowView: View {
    @Environment(\.isCompactLayout) private var isCompactLayout: Bool
    let title: String

    var body: some View {
        HStack {
// … (truncated for newsletter)
```

This shows a practical rule: use Environment for small, UI-scoped flags so ownership stays local and predictable instead of hidden in global state.

---

### Community Picks

**[Apple at 50](https://www.apple.com/)** — A reminder that platform expectations and developer ergonomics evolve slowly; architecture choices have long tails.  
**[Show HN: Dull – Instagram Without Reels, YouTube Without Shorts (iOS)](https://getdull.app)** — A lightweight product decision that echoes a technical one: fewer global services, smaller surface area to manage.  
**[Apple removes iPhone vibe coding app from app store](https://gizmodo.com/apple-removes-iphone-vibe-coding-app-from-app-store-2000740084)** — Operational risk still matters; validate runtime behavior and policy compliance early.

---

### Until Next Time

If you’re refactoring singletons into injectables this week, test the navigation paths that create and discard views and add signpost-style traces around resolver events. Read the full piece on DI patterns, try constructor injection for view-owned state, and hit reply with how you validated lifecycle across complex navigation in your app — I’ll share the best tactics next issue.
