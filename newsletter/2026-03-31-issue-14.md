## iOS Dev Weekly — Issue #14

### Opening

Local Xcode iterations that balloon are a real productivity tax — not a headline feature, but a daily drag for teams. Header reprocessing and flaky incremental behavior show up as mysterious compile/CI regressions that waste focus and time. This week: practical fixes to make builds predictable again.

---

### This Week's Big Story

**Speed Up Xcode Builds with Explicit Modules**

Missing per-target module flags and implicit umbrella headers cause repeated Objective‑C header parsing between incremental builds, slowing developer feedback loops. Making modules explicit reduces reprocessing, trims CI time under certain workloads, and cuts down on transient linker/import errors that only show up incrementally. If your team cares about fast local iteration, this is the kind of maintenance that pays compound dividends.

---

### Trend Signals

• Ollama running MLX on Apple Silicon in preview suggests more machine‑learning tooling will target native Apple chips, increasing expectations for local, fast developer workflows. [Source: HackerNews]  
• Cherri — a language compiling to an Apple Shortcut — highlights lightweight experimentations in the ecosystem; teams should expect more small tools that integrate directly with Apple platforms. [Source: HackerNews]  
• Apple cracking down on "vibe coding" apps and removing ‘Anything’ from the App Store signals stricter policy enforcement that impacts novelty app developers more than traditional teams. [Source: Reddit r/iOSProgramming]  
• Ongoing conversations about SwiftUI and data layers show practitioners are still weighing ergonomics against control, which matters when choosing architectures that influence build boundaries. [Source: Reddit r/iOSProgramming]

---

### Swift Snippet of the Week

```swift
import Foundation

struct ModuleCheck {
    let projectFile: URL

    func run() throws {
        let data = try String(contentsOf: projectFile, encoding: .utf8)
        // Split targets by block "/* Begin PBXNativeTarget section */" is brittle; instead match target buildSettings blocks
        let pattern = #"([A-F0-9]{24}) /\* (.+?) \*/ = \{(?:[^}]*?buildSettings = \{([^}]*?)\};)?"#
        let regex = try NSRegularExpression(pattern: pattern, options: [.dotMatchesLineSeparators])
        let ns = data as NSString
        var missing: [String] = []
        regex.enumerateMatches(in: data, options: [], range: NSRange(location: 0, length: ns.length)) { m, _, _ in
            guard let m = m, m.numberOfRanges >= 3 else { return }
            let name = ns.substring(with: m.range(at: 2))
            let buildSettingsRange = m.range(at: 3)
            let settings = buildSettingsRange.location != NSNotFound ? ns.substring(with: buildSettingsRange) : ""
            if !settings.contains("DEFINES_MODULE") || !settings.contains("DEFINES_MODULE = YES") {
                missing.append(name.trimmingCharacters(in: .whitespacesAndNewlines))
            }
        }
        if missing.isEmpty {
            print("All targets appear to define modules (DEFINES_MODULE = YES).")
        } else {
            print("Targets missing DEFINES_MODULE = YES:")
// … (truncated for newsletter)
```

This pattern codifies the practical check: if your project doesn’t consistently declare DEFINES_MODULE = YES, expect incremental build churn and flaky imports — fix it early.

---

### Community Picks

**[Ollama is now powered by MLX on Apple Silicon in preview](https://ollama.com/blog/mlx)** — Native Apple‑Silicon ML runtimes are becoming more common; consider local tooling impacts on developer machines.  
**[Cherri – programming language that compiles to an Apple Shortuct](https://github.com/electrikmilk/cherri)** — Small language experiments like this show the ecosystem exploring new automation and UX shortcuts for developers.  
**[Apple steps up crackdown on vibe coding apps, pulls ‘Anything’ from the App Store](https://reddit.com/r/iOSProgramming/comments/1s8gij4/apple_steps_up_crackdown_on_vibe_coding_apps/)** — A reminder that App Store policy changes can suddenly affect niche app categories.

---

### Until Next Time

If you maintain frameworks, start a staged rollout: enable DEFINES_MODULE per target, add module.modulemap where headers are nonstandard, and use a controlled CI clean build during the switch. Hit reply with how your team staged a modules rollout or what CI cache strategy caught a transient incremental failure — I’ll share practical follow‑ups next issue. Forward this to an engineer who spends too much time waiting on Xcode.
