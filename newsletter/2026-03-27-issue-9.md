## iOS Dev Weekly — Issue #9

### Opening

Converting completion-handler code to structured concurrency is showing up in our crash logs and slow CI jobs — not just as compile edits but as changed cancellation and lifetime semantics that surface at runtime. Treat these migrations as operational work: add tests that assert cancellation, instrument async boundaries, and stage rollouts so you don’t turn a refactor into an availability incident.

---

### This Week's Big Story

**Structured Concurrency Patterns for Swift 6.3**

Migrating callback-heavy flows to async/await can silently change who owns work, when children are cancelled, and whether network requests are torn down properly. Those differences are the reason you’ll see leaked tasks or UI races only after shipping. Plan tests, observability, and incremental rollouts as part of the rewrite.

---

### Trend Signals

• Swift 6.3 has shipped — expect continued refinement of concurrency ergonomics and runtime behavior that will affect large codebase migrations. [Source: Swift.org Blog]  
• Apple discontinues the Mac Pro, a sign that hardware focus is shifting and teams should expect platform tooling and CI footprint changes over time. [Source: HackerNews]  
• iOS 26.4 (the “Big Six” update) appears to be triggering app-level UX and compatibility work for many teams; compatibility testing should be part of your migration plan. [Source: Medium]  
• SwiftUI drawing and Canvas pieces are getting attention again; if you migrate view-layer async work, validate drawing and render-path cancellation under load. [Source: Medium]

---

### Swift Snippet of the Week

```swift
// ✅ Preferred: bounded, aggregate results
func fetchAll(_ urls: [URL]) async throws -> [Data] {
 try await withTaskGroup(of: Data?.self) { group in
 for url in urls {
 group.addTask {
 let (data, _) = try await URLSession.data(for: URLRequest(url: url))
 return data
 }
 }
 var results: [Data] = []
 for await maybeData in group {
 if let data = maybeData { results.append(data) }
 }
 return results
 }
}
```

This encodes an engineering judgment: bind child work to the caller, keep results aggregated and bounded, and avoid detached tasks that outlive the intended scope.

---

### Community Picks

More community links next issue.


---

### Until Next Time

If you’re migrating a feature, write unit tests that assert cancellation and add a focused Instruments run that watches for leaked tasks and active network sockets. Hit reply with the test or instrumentation that found your worst migration bug — I’ll fold useful examples into the next issue.
